"""
routers
-------
Sub-package containing one FastAPI ``APIRouter`` module per resource domain.

Modules
-------
products    — CRUD endpoints for the product catalogue (``/products``).
customers   — CRUD endpoints for customer management (``/customers``).
orders      — Order placement, retrieval, and cancellation (``/orders``).
dashboard   — Read-only aggregate summary endpoint (``/dashboard``).
"""
